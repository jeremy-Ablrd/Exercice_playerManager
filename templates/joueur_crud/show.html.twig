{% extends 'base.html.twig' %}

{% block title %}JoueurCrud{% endblock %}

{% block body %}
    <h1>JoueurCrud</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ joueur_crud.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ joueur_crud.nom }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ joueur_crud.numero }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ joueur_crud.Date ? joueur_crud.Date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_joueur_crud_index') }}">back to list</a>

    <a href="{{ path('app_joueur_crud_edit', {'id': joueur_crud.id}) }}">edit</a>

    {{ include('joueur_crud/_delete_form.html.twig') }}
{% endblock %}
